# INITIAL SIS-750-DATA-REPOSITORY
this repository contains code and outputs of select data analysis projects
single hashtags are big large font (this is a markdown document. As a `README.md` file it is the first thing visitors to the repo see


```
library(tidyverse)
ggplot(mtcars, aes(x = wt, y = disp))+
geom_point()
```

## Second Level Header
note: 'commit" is like 'save'; ('push' also acts similarly)
and then you have to add a commit message (especially important for collaborations, ie- "fixed these bugs")

# consider these short & sweet introductions...
